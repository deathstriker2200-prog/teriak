# 🚂 دیپلوی تریاکی روی Railway

آموزش قدم‌به‌قدم از صفر تا روشن شدن ربات — حدود ۱۰ دقیقه طول می‌کشه

---

## قدم ۰ — چی لازم داری

- اکانت [Railway](https://railway.app) (با گیت‌هاب لاگین کن)
- اکانت [GitHub](https://github.com)
- توکن ربات از [@BotFather](https://t.me/BotFather) → دستور `/newbot`

---

## قدم ۱ — پروژه رو ببر روی گیت‌هاب

```bash
unzip teriaky.zip && cd teriaky
git init
git add .
git commit -m "teriaky phase 1"
# یه ریپوی خالی تو گیت‌هاب بساز (بدون README) بعد:
git remote add origin https://github.com/USERNAME/teriaky.git
git push -u origin main
```

💡 فایل `.gitignore` جلوی رفتن `*.db` و `.env` رو می‌گیره پس دیتابیس و توکن لو نمیره

---

## قدم ۲ — ساخت پروژه تو Railway

1. تو داشبورد Railway بزن **New Project**
2. انتخاب کن **Deploy from GitHub repo**
3. ریپوی `teriaky` رو انتخاب کن

خود Railway با دیدن `requirements.txt` پایتونی بودنش رو تشخیص میده و با `railway.json` دستور استارت `python bot.py` ست میشه — هیچ کاری لازم نیست

> ربات با **Long Polling** کار می‌کنه پس به دامنه و پورت وب احتیاجی نیست — فقط پروسه زنده می‌مونه

---

## قدم ۳ — ساخت Volume (برای اینکه دیتابیس نپره) ⭐ مهم‌ترین قدم

SQLite روی دیسک فایل می‌سازه و دیسک Railway با هر ری‌دیپلوی ریست میشه — پس باید Volume بسازی:

1. برو تو **سرویس** ربات → تب **Settings**
2. پایین بیا تا **Volumes** → بزن **New Volume** (یا **Attach Volume**)
3. تو فیلد **Mount Path** دقیقا بنویس: `/data`
4. ذخیره کن — railway خودش ری‌دیپلوی می‌کنه

💡 **از این ورژن ربات خودش ولوم رو تشخیص میده و هیچ متغیر اضافه‌ای لازم نیس** — هم متغیر `RAILWAY_VOLUME_MOUNT_PATH` که خود ریلوی موقع اتچ ولوم می‌سازه رو می‌خونه هم `/data` رو چک می‌کنه و دیتابیس رو روی ولوم نگه می‌داره. تو لاگ استارت مسیر دقیق چاپ میشه (`DB: sqlite+aiosqlite:////data/...`)

اگه دیتات قبلا تو Volume نبوده و ریست شده: یه بار Volume رو بساز و کافیه — از این به بعد سر جاش می‌مونه

### بک‌آپ ادمین (خیلی آسون) 💾

بعد از اینکه آیدیتو تو `TERIAKY_ADMIN_IDS` گذاشتی:

- **`/backup`** → کل دیتابیس (بازیکن‌ها، تیم‌ها، سگ‌ها، همه چی) به‌صورت یه فایل `.db` برات می‌فرسته
- **`/upload_backup`** → بعدش فایل بک‌آپ رو برمی‌گردونی — اگه فایل سالم باشه **کل اطلاعات ربات عینا مطابق اون فایل جایگزین میشه** و چون رو Volume ذخیره میشه، ری‌دیپلوی هم نمی‌پره

این یعنی ری‌استور بعد از خرابکاری یا انتقال به سرویس جدید = دو دستور تلگرامی 😎

---

## قدم ۴ — تنظیم متغیرها (Variables)

توی سرویس → تب **Variables**:

| کلید | مقدار | وضعیت |
|---|---|---|
| `TERIAKY_TOKEN` | توکن ربات از BotFather | ✅ اجباری |
| `TERIAKY_ADMIN_IDS` | آیدی عددی تلگرامت — برای پنل ادمین و `/backup` و `/upload_backup` (با کاما چندتا) | ✅ پیشنهادی |
| `TERIAKY_DB` | `sqlite+aiosqlite:////data/teriaky.db` | ⭕ اختیاری — خودش ولوم رو تشخیص میده |

نمونه کامل متغیرها تو فایل `.env.example` توضیح داده شده

⚠️ اگه `TERIAKY_DB` رو دستی می‌ذاری: چهار تا اسلش لازمه — سه تا برای URL و یکی برای مسیر absolute. اگر `/data` ولوم نباشه و فایل هر بار ناپدید بشه یعنی مسیر اشتباهه

آیدی عددی تلگرامت رو می‌تونی از ربات [@userinfobot](https://t.me/userinfobot) بگیری

---

## قدم ۵ — تست

1. تب **Deployments** → لاگ‌ها رو باز کن
2. باید این دو خط رو ببینی:

```
دیتابیس آماده شد ✅
ربات تریاکی اومد بالا 🤖
```

3. برو تو تلگرام به ربات `/start` بزن 🎉

---

## قدم ۶ — فعال کردن گروه‌ها (مهم) ⚠️

تا «حمله» با ریپلای و دستورهای متنی مثل «خرید چاقو» تو گروه کار کنه باید Privacy Mode ربات خاموش باشه:

1. تو تلگرام برو به [@BotFather](https://t.me/BotFather)
2. بزن `/setprivacy` → رباتت رو انتخاب کن → **Disable**
3. اگه ربات رو قبلا به گروه اضافه کرده بودی: **حذفش کن و دوباره ادش کن** (تا تنظیم جدید اعمال بشه)

بدون این کار ربات فقط دستورهای اسلشی‌دار (`/`دار) رو تو گروه می‌بینه

---

## به‌روزرسانی بازی

```bash
git add . && git commit -m "update" && git push
```

Railway خودش اتوماتیک ری‌دیپلوی می‌کنه — دیتابیس تو Volume می‌مونه و هیچی پاک نمیشه

---

## نکته‌های مهم

- **دیتا فقط با حذف Volume پاک میشه** — پس مواظب دکمه Delete volume باش
- بکاپ آسون: از داخل تلگرام `/backup` (ادمین) — و ری‌استور با `/upload_backup` — دیگه نیازی به ترمینال نیس
- پلن Hobby ماهی ۵ دلار اعتبار رایگان میده — این ربات با polling خیلی کم مصرفه و راحت جا میشه
- **آپگرید به PostgreSQL در آینده**: برو بخش «مهاجرت به PostgreSQL» پایین همین فایل، اسکریپت آماده و تست‌شده ست

---

## مهاجرت به PostgreSQL (بدون از دست رفتن دیتای بازیکن‌ها)

اسکریپت `migrate_to_postgres.py` همین ریپو این کار رو می‌کنه: به SQLite قدیمی و Postgres جدید همزمان وصل میشه، هر ۱۳ جدول رو به ترتیب وابستگی (کاربر قبل از زمین/سگ/تیم) کپی می‌کنه، بعد از هر جدول تعداد ردیف مبدا و مقصد رو چاپ و مقایسه می‌کنه و آخرش دونه‌به‌دونه ردیف‌ها رو راستی‌آزمایی می‌کنه. اسکریپت idempotent عه: قبل از insert چک می‌کنه رکورد قبلا نیومده باشه، پس اگه وسطش قطع شد دوباره اجراش کن، دیتا تکراری نمیشه.

⚠️ نکته حیاتی: **قبل از شروع مهاجرت، سرویس ربات باید کاملا خاموش باشه** تا وسط کپی، بازیکنا چیزی روی SQLite قدیمی ننویسن و از قلم نیفته.

### چک‌لیست سوییچ نهایی (به ترتیب، شماره‌گذاری‌شده)

1. توی Railway دکمه **New → Database → PostgreSQL** رو بزن و صبر کن سرویس بالا بیاد
2. از تب **Variables** سرویس Postgres مقدار `DATABASE_URL` (یا `PGURL`/`POSTGRES_URL`) رو بردار و به این فرم دربیار (فقط پیشوندو عوض کن، رمز جایی تو کد نره):
   `postgresql+asyncpg://USER:PASSWORD@HOST:PORT/DBNAME`
   (فرم‌های `postgres://` و `postgresql://` ساده هم قبولن، اسکریپت و خود بات خودکار به `postgresql+asyncpg://` تبدیل‌شون می‌کنن)
3. **روی سرویس ربات دکمه ⋮ → Stop** رو بزن تا ربات کاملا خاموش بشه و هیچ نوشتن جدیدی روی SQLite انجام نشه
4. از رایانه خودت (یا Railway Shell) مهاجرت رو اجرا کن:
   ```bash
   TERIAKY_MIGRATE_FROM="sqlite+aiosqlite:////data/teriaky.db" \
   TERIAKY_MIGRATE_TO="postgresql+asyncpg://USER:PASSWORD@HOST:PORT/DBNAME" \
   python migrate_to_postgres.py --yes
   ```
   اگه دی‌بی روی ولوم Railway عه و دسترسی مستقیم به فایلش نداری، قبلش با `/backup` تو تلگرام فایل رو بگیر و کنار کد بذار و مسیرش رو بده (`--from teriaky-backup-XYZ.db`)
5. آخرش باید `✅ مهاجرت کامل و سالمه، می‌تونی سوییچ کنی` ببینی و هر جدول دو تا ✅ (شمارش + راستی‌آزمایی). اگه چیزی ❌ شد، دوباره همون اسکریپت رو اجرا کن (idempotent عه). حتما چند تا کاربر واقعی رو هم نمونه چک کن
6. توی Variables سرویس ربات، `TERIAKY_DB` رو به همون `postgresql+asyncpg://...` تغییر بده
7. سرویس ربات رو **Restart/Redeploy** کن تا بالا بیاد و به Postgres وصل بشه (تو لاگ باید `DB: postgresql+asyncpg://...` ببینی)
8. یه تست سریع بکن: `/start` پروفایلت نشون بده، موجودی و لولت همون باشه — بعدش کاربرای دیگه رو هم سر بزن
9. ولوم SQLite رو **چند روز نگه دار** (پشتیبان)، بعد که مطمئن شدی همه چی اوکیه می‌تونی حذفش کنی

🔒 امنیت: رمز پستگرس هیچ‌جا تو کد یا گیت‌هاب نیس و نباید باشه — همیشه از متغیر محیطی Railway استفاده کن. لاگ‌های اسکریپت هم رمز رو ماسک می‌کنن

## مشکل دیدی؟

| علامت | دلیل احتمالی |
|---|---|
| `توکن ربات پیدا نشد` | متغیر `TERIAKY_TOKEN` ست نشده |
| ری‌استارت مداوم | توکن اشتباهه — تو لاگ خطای `Unauthorized` میبینی |
| بعد ری‌دیپلوی همه چی ریسته | Volume نساختی — Mount Path باید دقیقا `/data` باشه — تو لاگ استارت چک کن نوشته `DB: sqlite+aiosqlite:////data/...` |
| دستورهای متنی تو گروه کار نمی‌کنه | Privacy Mode رو با `/setprivacy` خاموش نکردی یا بعد خاموش کردن ربات رو دوباره به گروه اد نکردی |
| `/admin` میگه ادمین نیستی | `TERIAKY_ADMIN_IDS` با آیدی عددی تو ست نشده |
| ربات جواب نمیده | Services → سرویس Sleeping نباشه — تو لاگ آخرین خط رو چک کن |
