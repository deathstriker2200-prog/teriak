# توکن ربات تلگرام از BotFather
TERIAKY_TOKEN=123456:ABC-your-token

# پیش‌فرض SQLite همین فایل محلیه — برای PostgreSQL مثلا:
# TERIAKY_DB=postgresql+asyncpg://user:pass@localhost/teriaky
TERIAKY_DB=sqlite+aiosqlite:///teriaky.db
